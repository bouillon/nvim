#!/bin/bash
# KDE Plasma clipboard copy wrapper
# Uses qdbus to access KDE's klipper directly

# Read from stdin
content=$(cat)

# Set clipboard via KDE's klipper
qdbus6 org.kde.klipper /klipper org.kde.klipper.klipper.setClipboardContents "$content" >/dev/null 2>&1
exit 0
